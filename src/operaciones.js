export const suma = (num1, num2) => {
return num1 + num2;
}

const resta = (num1, num2) => {
    return num1 - num2;
}

const mult = (num1, num2) => {
    return num1 * num2;
}


export const saludo = 'hola mi nombre es alfonso cruz, saludos desde el modulo';